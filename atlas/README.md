# Атлас человека — таксономия

Интерактивная карта знаний о человеке: от хромосом и гормонов до психики, общества,
культуры и тончайших черт личности.

**462 раздела · 36 960 позиций · 8 доменов**

## Как посмотреть

`Atlas.dc.html` — самодостаточный вьюер (карта + список + поиск). Открывается
по локальному HTTP (не по `file://` — браузер блокирует загрузку скриптов):

```bash
cd atlas
python3 -m http.server 8080
# и открыть http://127.0.0.1:8080/Atlas.dc.html
```

Вьюеру нужны `support.js` и `data.js` рядом. Интерфейс не менялся — только данные и счётчики.

## Устройство

| Файл | Что это |
| --- | --- |
| `Atlas.dc.html` | Интерфейс: карта-граф, список, поиск, боковая панель |
| `support.js` | Рантайм вьюера |
| `data.js` | Собранные данные — `window.__TAXONOMY__` |
| `src/data/*.ts` | Исходники таксономии |
| `tools/build-data.cjs` | Сборка `data.js` из исходников + проверка целостности |
| `src/App.tsx` | Альтернативный вьюер на React/Vite |

### Формат

```ts
interface Section { id, title, subtitle, icon, color, level, entries: Entry[] }
interface Entry   { id, title, description, detail?, male?, female?, tag? }
```

Поле `level` — не украшение: по нему вьюер раскладывает разделы по восьми доменам
и по подгруппам внутри них (`_domainOf` в `Atlas.dc.html`). Новый раздел с незнакомой
строкой `level` уедет в домен «Психика и личность» по умолчанию.

### Слои исходников

- `sections.ts` — базовые разделы и сборка всего вместе;
- `extra.ts` … `extra11.ts`, `traits.ts` — исторические слои;
- `extra12.ts` — **культура и смысл**: сакральное и десакрализация, эвристики,
  риторика, язык и прагматика, нарратив, эстетика, время, пространство, дар,
  честь и стыд, тайна, игра, смерть, тело в культуре;
- `extra13.ts` — **психика**: метапознание, решения, память, внимание, привычки,
  регуляция эмоций, мотивация, самообман, идентичность, творчество, обучение,
  мудрость, наука об отношениях, команды;
- `extra14.ts` — **общество**: бюрократия, право, поведенческая экономика, деньги,
  труд, образование, здоровье, наука и техника, риск, миграция, платформы, война,
  коррупция, экология, неравенство;
- `extra15.ts` — **доктрина и формула**: доктрина, догма, ортодоксия;
  казуистика и разбор случая; толкование нормы; дипломатическая формула;
  признание, суверенитет, статус; стратегические доктрины; серая зона;
  язык власти и его дешифровка; идеологический контроль; переговоры;
- `extra16.ts` — **насилие, миф и память**: типология насилия (Галтунг, Жижек,
  Арендт, Беньямин); символическое насилие (Бурдьё); эпистемическое насилие
  и эпистемическая несправедливость (Спивак, Фрикер); институциональное
  и административное насилие; структурное и медленное насилие (Никсон,
  Мбембе, Батлер); политический миф (Сорель, Кассирер, Барт); образ врага
  и дегуманизация (Шмитт); отрицание и оправдание вреда (Коэн, Сайкс и Матза,
  Бандура); политика памяти; сопротивление и ненасильственное действие
  (Шарп, Скотт, Ченовет);
- `extra17.ts` — **понятийный аппарат**: значение (денотат, коннотат,
  сигнификат, экстенсионал); интенция, аттитюд и апперцепция; идеологема,
  аксиологема и кредо; инвариант, конструкт и постулат; виды императивов
  и их формулы; термин, дефиниция и тезаурус; ценз, допуск, правоспособность;
- `extra18.ts` — **пол, связь, политика**: модели полового ответа и согласие
  как процедура; формы союза и договорённости; уровни различения пола
  и гендера; партии и партийные системы; избирательные формулы и их
  последствия; повестка и цикл решения; устойчивость и слом режимов;
  представительство и легитимность;
- `extra19.ts` — **органы и учреждения**: парламент, палаты и фракции;
  сравнительный ряд названий собраний — дума, вече, сейм, тинг, кортесы,
  меджлис, курултай, ландсгемайнде; комитеты, комиссии и коллегии;
  процедура заседания, чтений и голосования; ведомства — министерства,
  службы, агентства; советы и коллегиальные органы; русская
  институциональная лексика от посадника до совнаркома; надзор,
  контроль и органы юстиции;
- `additions.ts` … `additions7.ts` — добор позиций **в уже
  существующие разделы**. Это словари вида `{ "id-раздела": [[заголовок, описание], …] }`;
  записи дописываются в конец своего раздела, нумерация продолжается автоматически.

## Пересборка данных

```bash
cd atlas
npm install          # esbuild приходит вместе с vite
npm run build:data   # то же самое: node tools/build-data.cjs
```

Скрипт падает с ненулевым кодом, если найдёт разрыв в нумерации, пустое поле,
повтор `id` раздела или повтор заголовка внутри одного раздела.

## Правила пополнения

1. **Сначала ищем существующий раздел.** Новый раздел заводится, только если позиции
   действительно некуда положить.
2. Заголовок уникален внутри своего раздела. Одно и то же понятие может встречаться
   в разных разделах под уточняющим заголовком — например, «Ирония (фигура речи)»
   рядом с «Ирония» в разделе про юмор.
3. Описание — одно предложение, которое объясняет суть, а не пересказывает заголовок.
4. Новый раздел получает `level` из уже существующего набора строк, иначе он выпадет
   из своего домена.

## Домены

| Домен | Разделов | Позиций |
| --- | ---: | ---: |
| Черты человека | 79 | 19 876 |
| Психика и личность | 141 | 8 312 |
| Общество | 117 | 4 299 |
| Культура и смысл | 50 | 2 273 |
| Тело и биология | 38 | 935 |
| Пол, любовь, связь | 17 | 536 |
| Навыки и призвание | 11 | 479 |
| Жизненный путь | 9 | 250 |
